<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mtg.mypbz2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyPBZ2">
        <activity android:name=".view.searchActivities.CheckInspectionActivity"></activity>
        <activity android:name=".view.searchActivities.CheckPoliceActivity" />
        <activity android:name=".view.searchActivities.NumberOfCarActivity" />
        <activity android:name=".view.editActivities.SearchEditActivity" />
        <activity android:name=".view.editActivities.AddInspectionActivity" />
        <activity android:name=".view.editActivities.AddPoliceActivity" />
        <activity android:name=".view.editActivities.AddCarActivity" />
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>